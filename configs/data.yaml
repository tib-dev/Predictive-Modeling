data:
  data_dir: "data/"
  raw_dir: "data/raw/"
  processed_dir: "data/processed/"
  reports_dir: "reports/"
  plots: "plots/"
  exports: "exports/"

logs:
  logs_dir: "logs/"

models:
  model_dir: "models/"
  checkpoints: "models/checkpoints/"

artifacts:
  artifacts_dir: "artifacts/"
